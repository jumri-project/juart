name: pytorch-cuda-121
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - pytorch::pytorch
  - pytorch::pytorch-mutex=[build=cuda]
  - pytorch::pytorch-cuda=12.1
  - ray-default
  - pip:
      - torchkbnufft
